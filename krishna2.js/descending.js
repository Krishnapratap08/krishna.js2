// Define an array of numbers (or strings) to sort in descending order
const arr = [5, 3, 8, 1, 4, 9, 2, 7, 6];

// Sort the array in descending order
arr.sort((a, b) => b - a);

// Print the sorted array
console.log(arr); // Output will be: [9, 8, 7, 6, 5, 4, 3, 2, 1]